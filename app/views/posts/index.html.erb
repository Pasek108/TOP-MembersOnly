<h1>Posts</h1>

<% @posts.each do |post| %>
  <div>
    <b><%= current_user ? post.user.username : "anonymous" %></b>
    <div><%= post.body %></div>
  </div>
<% end %>

<br>
<%= link_to "Create new post", new_post_path %>
<br><br>
